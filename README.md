# Limitation-of-ChatBot
Finding the limitations of chatbots (generic responses, hallucinations, lack of domain grounding, memory failures) and quantifying improvements/limitations using an offline RAG + memory system.
